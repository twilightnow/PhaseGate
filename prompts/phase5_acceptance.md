# Phase 5: Acceptance

Follow PHASE_5 steps from `docs/01_workflow_phases.md` (already loaded as context via `.phasegate/progress.md`).

Read `.phasegate/requirements/*.md` for the Acceptance Criteria list.
Run all module tests. Run constraint-checker.
Verify each acceptance criterion.

Output verification results. If all auto-verifiable criteria pass, generate `acceptance-guide.md`
listing the items requiring human verification.
